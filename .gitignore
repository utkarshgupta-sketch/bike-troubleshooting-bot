# Secrets — NEVER commit
.env

# Cache: commit the prebuilt manual indexes (fast cloud startup), ignore the rest
cache/*
!cache/*.faiss
!cache/*.chunks.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# macOS
.DS_Store
